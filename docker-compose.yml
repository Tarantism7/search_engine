services:
  db:
    image: mysql:latest
    container_name: java_search_engine
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: pass
      MYSQL_DATABASE: search_engine
    volumes:
      - db-data:/var/lib/mysql
volumes:
  db-data:

